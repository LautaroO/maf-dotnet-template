# Copy only settings you intentionally want at project scope to .codex/config.toml.
# Codex loads project-scoped config only for trusted projects.
# Keep authentication, provider credentials, and machine-specific settings out of the repository.

# Example project-instruction budget. Verify against your installed Codex version before enabling.
# project_doc_max_bytes = 32768

# Do not commit secrets or personal auth state.
